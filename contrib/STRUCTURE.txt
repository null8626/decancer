numerical_array offset (u16)
misc_case_sensitive offset (u16)
misc offset (u16)
alphabetical_pattern offset (u16)
alphabetical offset (u16)
similar offset (u16)

numerical_array:
  size of array (u8)
  array (u32 array)

misc_case_sensitive:
  size of table (u8)
  
  loop:
    size of translation (u8)
    translation string in ASCII (u8 array)
    size of confusables (u8)
    array of confusables (u32 array)

misc:
  size of table (u8)
  
  loop:
    translation in ASCII (u8)
    size of confusables (u8)
    array of confusables (u32 array)

alphabetical_pattern:
  size of array (u8)
  array (u32 array)

alphabetical:
  loop[26]:
    size of array (u8)
    array (u32 array)

similar:
  size of array (u8)

  loop:
    size of similar chars (u8)
    array of similar chars (u8 array)